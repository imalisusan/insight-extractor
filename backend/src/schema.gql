# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Submission {
  id: ID!
  originalFilename: String!
  geminiSummary: String!
  geminiKeyPoints: String!
  userEditedSummary: String
  userEditedKeyPoints: String
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Query {
  submissions: [Submission!]!
  submission(id: ID!): Submission!
}

type Mutation {
  analyzeFile(fileId: String!, analysisType: AnalysisType!): Submission!
  updateSubmission(updateSubmissionInput: UpdateSubmissionInput!): Submission!
}

"""The type of analysis to perform on the uploaded file"""
enum AnalysisType {
  SUMMARIZE
  EXTRACT_KEY_POINTS
}

input UpdateSubmissionInput {
  id: ID!
  userEditedSummary: String
  userEditedKeyPoints: String
}